{% extends 'base.html.twig' %} {% block title %} Sciety {% endblock %} {% block
body %}
<h1>Bem-vindo</h1>

<a href="{{ path('app_register') }}" class="btn btn-primary">Register</a>
<a href="{{ path('app_login') }}" class="btn btn-info">Login</a>
{% endblock %}
